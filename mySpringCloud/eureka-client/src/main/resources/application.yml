server:
  port: 8663 #运行端口号
spring:
  application:
    name: eureka-client #服务名称
eureka:
  client:
    register-with-eureka: false #注册到Eureka的注册中心
    fetch-registry: true #获取注册实例列表
    service-url:
      defaultZone: http://localhost:8666/eureka/ #配置注册中心地址
service-url:
  user-service: http://user-service
